<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="itesm.mx.mipasadoenpresente">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InformacionActivity" />
        <activity android:name=".EditarInfoActivity" />
        <activity android:name=".CategoriasPersonasActivity" />
        <activity android:name=".CategoriasEventosActivity" />
        <activity android:name=".PersonasListActivity" />
        <activity android:name=".EditPersonaActivity" />
        <activity android:name=".EventosListActivity" />
        <activity android:name=".EditEventoActivity" />
        <activity android:name=".dificultadActivity" />
        <activity android:name=".PersonaInfoActivity" />
        <activity android:name=".juegoActivity" />
        <activity android:name=".ConfigurarJuegoActivity"></activity>
    </application>

</manifest>